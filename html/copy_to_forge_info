#!/bin/bash
FORGE_DIR=/ipsl/forge/projets/ioserver/trac/htdocs/XIOS_TEST_SUITE
forge_IP=ioserver@forge.ipsl.jussieu.fr

branch_name=$1
files="compile_${1}_info.js test_${1}_info.js"

for file in $files 
do
  scp $file $forge_IP:$FORGE_DIR 
  ssh $forge_IP "chmod a+rw ${FORGE_DIR}/${file}"
done
